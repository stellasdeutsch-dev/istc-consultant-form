/* Published form definition.

   Replace this file with the one downloaded from admin.html
   ("Publish" button) to change the live form for everyone.
   While it is null, the built-in default in schema.js is used. */

window.PUBLISHED_SCHEMA = null;
